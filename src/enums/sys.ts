export enum SYS_MODE_ENUM {
    PHONE = 'phone',
    WEB = 'web',
}

export enum MENU_TYPE_ENUM {
    VERTICAL = 'vertical',
    CLASSIC = 'classic',
    TRANSVERSE = 'transverse',
    COLUMNS = 'columns',
}