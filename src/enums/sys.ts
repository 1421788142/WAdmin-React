export enum sysModeEnum {
    phone = 'phone',
    web = 'web',
}

export enum menuTypeEnum {
    vertical = 'vertical',
    classic = 'classic',
    transverse = 'transverse',
    columns = 'columns',
}