export enum STATUS_TEXT_ENUM {
    done = '上传成功',
    uploading = '上传中',
    loading = '待上传',
    error = '上传失败',
    removed = '已删除'
}

export enum STATUS_ENUM {
    done = 'success',
    uploading = 'active',
    loading = 'active',
    error = 'exception',
    removed = 'exception'
}